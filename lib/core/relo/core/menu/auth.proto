syntax = "proto3";

package relo.core.menu;

option go_package = "github.com/reloclass/core/menu";

// 菜单权限
message Auth {
  string name = 2;
  // @gotags: json:"moduleAuth"
  repeated Module modules = 3;
}

// 模块权限
message Module {
  string name = 2;
  bool read = 3;
  bool write = 4;
}
