syntax = "proto3";

package relo.core.userlecture;

option go_package = "github.com/reloclass/core/userlecture";


// 用户完成的讲次类型
enum Type {
  // 讲次
  LECTURE = 0;
  // 作业
  HOMEWORK = 1;
  // 测验
  TEST = 2;
}
